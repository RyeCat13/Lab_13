import math
from statistics import median

def total(values):
    return sum(values)

def average(values):
    return (sum(values)/len(values))
    with self.assertIs(0):
        return math.nan
    

def median(values):
    return median(values)
    with self.assertIs(ValueError):
        result = grades.median([])
